server:
  port: 8082

spring:
  application:
    name: member-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

info:
  app:
    name: member-service
    version: 1.0.0
