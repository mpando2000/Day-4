server:
  port: 8081

spring:
  application:
    name: claim-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

info:
  app:
    name: claim-service
    version: 1.0.0
